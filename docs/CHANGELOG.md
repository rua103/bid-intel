# 变更与决策记录

> 按日期倒序。这里记的是**经过**、**边界**和**当时的数字**。
> 可复用的结论（怎么跑、红线、哪些坑不要再踩）在 [`ONBOARDING.md`](ONBOARDING.md)，本文不重复。
> 开放问题在 [`GAP_ANALYSIS.md`](GAP_ANALYSIS.md)。

---

## 2026-09-26 · 两位队友的低误操作标注任务

给 1038 条已完成的全量后台任务建立可直接交接的人工标注工作流。新增本地分层采样器，默认生成 24 条任务：两位标注员先各自完成相同 6 条试标，再分别完成互不重叠的 9 条正式样本；每份 bundle 带空白 Gold、对应规则+OCR 预测、解析文本、原件相对路径和 SHA-256。原件副本与解析全文只写入 `backend/.data/annotation-tasks/`，不会进 Git；复用解析缓存 165 个文档，缓存未命中 24 个按原任务设置重解析，未调用模型，24 条均有来源文本。

- 按真实数据将“正文候选”和“无配对 ZIP”分开统计：无 ZIP 仅 68 条且只有 2 条有候选；机器识别多包只有 3 条，抽样不硬凑 4 条。零候选不等于原文无标的，源文告警需人工核验。
- 工作台可以直接载入任务包；每条公告单独留核验快照，改字段或改包后自动退回待核。只导出已核验的公告；大型来源文字改存 IndexedDB，并提供进度备份。
- 新增试标差异比较命令和安全 Gold 合并命令。后者允许单个队友任务对应完整 canonical predictions 的子集；最终可加 `--require-complete` 检查 24 条是否齐全。重复试标只报告差异，不自动投票或仲裁。
- 追加独立 `/annotation.html` 队友工作台和一页操作卡；分配脚本生成每位队友可直接分发的 ZIP，只含该阶段 bundle 与原件。工作台允许按原文增删包，并阻止重复/空包号、全空标的行、空主体名称和非法金额通过核验。比较器/合并器报告而不拦截 Gold 与预测的包结构差异，确保忠实金标可用于评分。
- 验证：后端全量 **167 passed、1 skipped**，Ruff 通过；前端 **11 passed**，生产构建通过并包含独立标注页（仍有现存 ECharts 大 chunk 提示）。

本机最终分发样本在 `backend/.data/annotation-tasks/official-20260926-team-ready/`。Gold 仍需队友对照原文产生；采样、解析文本和自动预测都不代表准确率。未覆盖第二台物理电脑的局域网访问验收。

---

## 2026-09-26 · 官方数据全量 rules + OCR 处理

为尽早验证大包、OCR 与续跑，在独立数据集 **“官方数据全量（规则与本地OCR·待核验）”** 处理 1038 条公告。任务 ID `47508ea8390b4522a6766d79c0188b5d`，数据集 ID `282bf10d09b44260942269fcd4242026`。全程 `rules + RapidOCR`、3 个工作进程、真实模型调用 0；未触碰默认业务库，也未改动官方原件。

- 最终 1038/1038 条检查点完成，0 失败；数据库 1038 条公告、1038 个导入回执、6814 条标的候选。投标参与和中标记录均为 0，不能把这轮结果当作完整主体抽取或准确率。
- 解析 14174 个叶文件（含 HTML）；970 个外层 ZIP 共 3,568,928,502 字节。RAR/7z 盘点数为 32/2，解析链路分别得到 239/13 个叶文件。RapidOCR 输出 2261 条识别/人工核验提示，覆盖 359 条公告；21 条无可信文本提示覆盖 16 条公告。
- 2008 个输入文件、合计 3,604,772,811 字节，按任务清单 SHA-256 复核全部匹配。首轮检查点有 13 条 Windows `PermissionError`；另外 8 条外层 ZIP 被误按 PDF 解析。修复状态读写锁及格式判定后续跑这 21 条，最终零失败，8 条误判已重解析。
- 最终仍有 87 个无效下载附件（61 条公告）、3 条公告含不支持格式警告、6 条公告含 PDF 流解析错误，另有 1 个压缩成员读取失败。错误附件已隔离，没有恢复缺失源内容；OCR 输出、候选和剩余附件问题均需人工核验。
- 依据逐条检查点时间估算，首轮约 65 分 16 秒，恢复约 1 分 29 秒，活跃处理墙钟合计约 66 分 45 秒。该值是 3 进程本机规则/OCR 工程基线，不是赛事分数或模型耗时。
- 验证（当时的全量处理提交前检查）：后端 **149 passed、1 skipped**，Ruff 通过；前端 **4 passed**，生产构建通过（仍有既有 ECharts 大 chunk 提示）。当前测试基线见标注工作流记录。

机器摘要：[official-full-20260926.json](benchmarks/official-full-20260926.json)。本次结果没有人工 gold，不生成准确率指标。旧的 `official-intake-20260925.json` 与 `official-fixes-20260925.json` 保持原样，分别作为首轮盘点和 6 条回归历史记录。

---

## 2026-09-25 · 官方附件兼容、错误隔离与重复候选修复

本轮处理 GAP 1.16/1.17/4.14 中已复现的首批问题，保留首轮记录。代码按真实内容分派格式，解封装 gzip 后隔离下载错误；DOC 改为默认 120 秒转换、专属配置复用、跨进程锁和成功内容缓存。补明确采购文件/需求表/未填模板的角色判定及保守跨文件去重；补成交报价表的规格说明、品目分类和明确包号。

实测证据：

- 87/87 个已知下载错误被识别；104 种 OLE 唯一内容重新核验，覆盖 2234 DOC、1 XLS，2172 个伪 DOCX 均判为 DOC。未转换全部 103 种 DOC。
- 两份原超时 DOC 首次转换 41.151/23.767 秒，再读缓存 0.251/0.041 秒；一份仅读出 3 字符，不能据此声称提取完整。
- 6 条附件回归的解析异常由首轮 12 个降为 0，无扩展名“不支持”由 2 降为 0；其中 1 个错误源文件已隔离、1 个招标 DOC 按角色跳过并另测转换，不能把它们称为有效附件已恢复。
- 候选总数 9→4：排除 4 条需求记录、合并 1 组重复。识别 7 份空模板；另 1 组品牌冲突保留、2 张图片仍待 OCR。所有合并保留来源证据，不自动合并不同包或歧义行。
- 57 文件公告首次修复后处理 243.179 秒（首次转换 9 个原失败 DOC），最终缓存复跑 1.631 秒；不是全量性能指标。两个阶段输出均保留。
- 后端 **139 passed, 1 skipped**（外部 Neo4j），**Ruff 通过**。新增 25 项回归；真实 DOC 用例增加伪扩展和缓存复用验证。前端未改，未重复其构建。

最终 6 条/4 候选写入独立“官方数据修复后回归（规则基线·待核验）”数据集，ID dd7e07c2b27f4897b67a934beef4fc39；默认库文件 SHA-256 不变，旧试跑结果保留。没有真实模型调用，没有改动原件，没有 gold 或准确率结论。机器记录见 [official-fixes-20260925.json](benchmarks/official-fixes-20260925.json)，操作边界见 [官方接入检查](OFFICIAL_INTAKE_REVIEW.md)。

截至 2026-09-25，RAR/7z、专有格式、大包、真实 OCR、进度/续跑及全量导入仍未完成；DOC 缓存暂无自动容量淘汰；XLS 参考工作表仍会占上下文；材料角色与品牌别名尚未在全量人工核验。次日全量批处理进展见上文。源数据缺失仍须由数据方处理，不能用模型补造。

---

## 2026-09-25 · 官方材料到货与首轮盘点

在本地官方数据根目录发现 1038 条 HTML、970 个同名 ZIP，原始 ZIP 共 3.57 GB。外层目录全部可读、全部配对；记录外层 ZIP 和正文 SHA-256，原始材料不移动、不替换、不入 Git。

- 递归检查 13650 个成员，末层记录 12998 个；RAR/7z 未展开、2 个嵌套容器触及本次检查限制，未做全量 CRC/文档解析。
- 已识别至少 87 个下载错误响应，涉及 61 条公告，含 48 个验证码页、31 个维护页、2 个限制页、6 个 JSON 错误。初报 39 个漏掉验证码页，复核正文后更新为 87，不能沿用旧数字。
- 2172 个 DOCX 经 OLE 流检查确认实际为旧 Word，涉及 212 条公告；另见无扩展名材料和超大包。
- 1038 条 HTML 规则解析耗时 41.11 秒，398 条有候选、640 条无候选，共 3814 个候选；这是正文产出统计，未经金标验证，不含附件和入库时间。
- 6 条正文＋附件试跑共 9 个候选，已存入独立“官方数据首批兼容性检查（规则基线·非完整结果）”数据集。发现 12 份文档解析失败、2 个无扩展名附件被跳过，真实 DOC 超时和跨文件重复问题均已复现。

新增 GAP 1.16/1.17/4.14，4.3 标为首轮盘点完成、全量待接入。全部处理为 rules、模型调用 0；本轮没有改应用代码，也没有重新宣称准确率或全量吞吐。源文件完整异常清单仅保存在本地 .data，仓库新增聚合统计与 [官方接入检查](OFFICIAL_INTAKE_REVIEW.md)。未重新运行不受影响的应用测试；此前实现基线仍为 114 passed、1 skipped、Ruff 通过、前端 4 passed。

---

## 2026-09-25 · 数据到达前的三项接入准备

完成 P1-7、GAP 4.11、P1-6 的本地实现与回归。操作说明见 [DATA_INTAKE.md](DATA_INTAKE.md)。

实现提交：[1895a26](https://github.com/rua103/bid-intel/commit/1895a260c543dd8b7b468fe090da8a8e4ec6235d)。缺口状态、接入说明、测试数字与验收边界随实现提交；实际提交号在后续文档提交补记。

| 修改 | 验证与边界 |
|---|---|
| 旧 XLS 用 xlrd 解析；PDF 表格、渲染与 Pillow 成为基础依赖 | 实际 XLS 多工作表、数值零单价；中文带框 PDF 的七字段及页码来源通过。扫描页真实渲染为 PNG，但 OCR 引擎使用模拟，未测真实扫描识别质量 |
| DOC 用隔离 LibreOffice 转 DOCX，超时终止该次进程树 | Word 16 保存的二进制 DOC 七字段完整；缺工具、转换超时和真实超时进程终止已测；HTML 伪扩展识别通过 |
| ZIP 文件名逐成员恢复编码，报告异常 | GBK 与 UTF-8 混合、嵌套附件归属、无标志 UTF-8、Unicode Path CRC、重复文件名、损坏 ZIP 和有效公告并存已测；非中文任意代码页不保证自动判断 |
| 每数据集独立 SQLite，通过 X-Dataset-ID 选择 | 默认库保留；所有五查询、图谱、单条/批量导入均按所选库读写；同名主体及 ID、不同行为金额、并发请求、重启和非法 ID 已测 |
| 前端新建/切换数据集、能力提醒、逐公告警告 | 旧响应不会覆盖新数据集；导入中固定所选库；Edge 无界面浏览器实际完成新建→导入样例→切换默认空库→切回→刷新，脚本错误 0 |

实测：后端 **114 passed, 1 skipped**（跳过项为外部 Neo4j）；Ruff **All checks passed**；前端 **4 passed**，生产构建成功（保留既有 ECharts 大 chunk 提示）。新增附件 15 项、数据集 7 项，共 22 项后端回归。运行环境 Python 3.13.7，LibreOffice 26.2.6.3。

验证发现的边界：LibreOffice 自身导出的某份 DOC 在 Word 中有表格，但由 LibreOffice 回读时变连续文字；Word 保存的同内容可正确回读。代码对 DOC 有文字但无标的表格发出明确警告，不能把转换进程成功退出当作字段提取成功。初次全量回归因此失败，定位后改用 Word 保存的格式夹具完成七字段验收；没有将失败结果计作通过。

本轮仅使用合成格式夹具和临时隔离数据库，没有调用付费模型或改动现有开发库。LibreOffice 官方 MSI 经 SHA-256 校验后在项目本地管理解包，未设为系统 Office；安装文件、运行工具和临时浏览器验证库均受 .data 忽略规则保护。迁移机器须重新部署依赖。

截至 2026-09-25 仍未完成：真实扫描 OCR、官方复杂附件全覆盖、1000 条真实端到端耗时、后台任务/断点续跑、P1-1 投标主体、P1-3～5 提交物/演示/登录。不能因当时的合成回归而承诺官方数据当天跑完；2026-09-26 的真实全量结果见本文件顶部记录。

---

## 2026-09-25 · 收口两条悬挂分支

远端积了两条各自从 `a95be2f` 分出的单提交分支，**都落后 main 10 个提交**。处理方式不同，因为风险不同。

### `origin/main2` — 直接合并 ✅

`52d4359 feat: add examples and synthetic test data`，452 行纯新增（`examples/gold.reviewed.json`、`examples/predictions.json`、3 个合成公告 HTML）。试合无冲突，且这些路径在 main 上都不存在。已 `--no-ff` 合并。

### `origin/code` — **不合并**，只挑拣 ⚠️

`83c1d6c fix: preserve reviewed gold and show field errors`，试合**冲突**。更危险的是：它的"顺手解法"会**静默回退两个 P0 修复**——

| 分支里的改动 | 会覆盖掉 | 后果 |
|---|---|---|
| `uid` 改回 `crypto.randomUUID()`、删掉 `import { annotationId }` | P0-5 | 局域网 http 下标注按钮全部抛异常 |
| 删掉 `confirmIdenticalGold` | P0-3 | gold 与 predictions 完全相同时不再需要显式确认 |
| 提示语改回「已生成 N 条待核验草稿」 | P0-3 | 掩盖"gold 是空白表、预测单独存"的语义 |

**根因**：它从 `a95be2f` 分出去时 P0 工作还没发生。在它的上下文里"改回原样"是中性操作，在现在的 main 上却是回退修复。

**移植了什么**（`AnnotationWorkbench.vue`）：

- `fieldErrors` 计算属性 + `findItem` 辅助函数——把 `report.alignments` 里 `wrong`/`missing`/`extra` 的字段摊平成表格
- 评测结果区新增「逐字段错误」表：公告 / 采购包、字段、错误类型、**人工答案 vs 系统答案**并排
- `watch(predictions)` —— 只导入预测 JSON 也能持久化到 localStorage

**没移植什么**：`uid`、`confirmIdenticalGold`、提示语，以及 `restoreReviewStatus`/`setReviewed`（那套"导入已核验 gold 就恢复勾选"的做法与 P0-3 的保守方向相反——main 现在会在任何 gold 变动后强制重新核验）。

### 验证

- 后端 `92 passed, 1 skipped`；ruff `All checks passed`
- 前端 `npm test` 2 passed；`npm run build` 通过
- 显式确认 P0 修复仍在：`annotationId` 2 处、`confirmIdenticalGold` 4 处、`allow_identical_gold` 1 处、直接调用 `crypto.randomUUID()` **0 处**

### 教训

**分支落后越多，合并时越容易把已修的 bug 带回来**，而且冲突解决看起来是"顺手"的。收口前必须逐条确认：这个 diff 相对**当前** main 会改掉什么，而不是相对它的分叉点。上面那张表就是这么查出来的——用 `git diff origin/main origin/code`，不是 `git diff <分叉点> origin/code`。

---

## 2026-09-25 · P0-1～P0-7 修复与验收

本次完成缺口清单中的 P0-1～P0-7。

> ⚠️ **一个教训**：首轮修改后虽有 71 项测试通过，但测试未覆盖多包同名、重复表头和真实头部混排行，因此当时"P0 全部完成"的结论**过早**。本轮先复现这些反例，再补修和验证。

### 修改与验收边界

| 条目 | 当前行为 | 验证 |
|---|---|---|
| P0-1 合并 | 只对原始规则/模型行做一对一对齐，明确且相等的包号优先。缺包号时利用名称、型号和数值兼容性，仅双方唯一时合并，并补回明确包号。无法唯一确定时保留候选并警告，不按模型返回顺序丢行 | 两包同名、模型顺序互换、已知包优先、歧义保留、规则标的与模型中标方同包入库 |
| P0-2 非标的过滤 | 完整表头别名匹配；中途重复表头跳过并更新列映射；嵌套表格只读取自己的行；过滤章节/联系行和"见附件"占位 | 重复/换序表头、嵌套布局表、联系块、附件占位；合法名称"3.0T 磁共振设备"仍保留 |
| P0-3 评测 | gold 为独立空白表，预测单独保存；全空 gold 拒绝评测。完全相同内容默认拒绝，忽略 ID、顺序和小数尾零差异；确实独立核验的完美结果需显式确认，并在界面、JSON 和 Markdown 中保留声明 | `/draft`→`/run` 接口测试：空草稿、复制预测、合法完美匹配、纠正字段后的非满分结果 |
| P0-4 局域网 | 默认使用页面主机名和端口 8000；可用 `VITE_API_BASE` 覆盖；后端支持私网来源和 `CORS_ALLOWED_ORIGINS` 指定域名 | CORS 预检及私网边界测试；前端主机名/地址覆盖测试 |
| P0-5 标注 ID | HTTP 环境无 `randomUUID` 时使用本地时间、计数和随机后缀产生记录 ID | 4 类操作共 400 个 ID 无重复；crypto 缺失及原生 UUID 分支均通过 |
| P0-6 标准列名 | 补充"名称""标的名称""采购标的名称""金额(元)"等；不把"品目号"当品目名称；金额按表头或单元格单位换算，百分比/引用不猜金额 | CCGP 表头变体、万元表头、零金额、百分比、附件引用测试 |
| P0-7 头部元数据 | HTML 文本保留换行及单元格分隔；优先读精确键值单元格，后用有冒号的行；主公告优先于附件，不在压平全文上猜无冒号字段 | 两列/四列头部、空单元格、嵌套段落、章节边界、附件顺序；20 条本地真实公告对照 |

### 本地真实公告与旧模型回放

机器记录见 [`benchmarks/p0-verification.json`](benchmarks/p0-verification.json)。该文件只含公开公告文件名、SHA-256、核对布尔值和计数，没有本地密钥或不可移植的数据路径。原始公告在本地开发集内，未提交 Git。

- 20 条公告的项目名称：**20/20** 与头部单元格一致。
- 采购单位：**20/20** 一致。
- 总中标金额：**20/20** 与头部数值按元换算后一致。原网页若填了 0 或进行了四舍五入，保留原文值；不据此声称与正文金额一致。
- 修复后规则路径共返回 **14 条**候选，仍不覆盖所有非表格/附件内容。**该计数不是召回率。**

用 `stream-compare-3.json` 中已保存的模型结果回放合并，**没有重新请求模型**：

| 公告标识 | 当前规则 | 已保存模型 | 合并后 |
|---|---:|---:|---:|
| `912477ae89e3b441` | 1 | 1 | 1 |
| `78552f907a9bc527` | 3 | 3 | 3 |
| `031f976924d449e1` | 0 | 15 | 15 |

第二条合并后保留 `合同包1`；第三条不再输出旧规则中的章节、联系人或包汇总行。原文没有标的总价时仍留空，不将"未填"一律当成解析 bug，不从采购包金额分摊。

### 检查结果

- 后端全量：**92 passed, 1 skipped**。跳过项为需要外部环境的 Neo4j 测试。
- Ruff：**All checks passed**。
- 前端 `npm test`：**2 passed**；`npm run build`：通过。
- `git diff --check`：通过。已有的前端大包提示和 Starlette 弃用提示仍存在。
- 本轮新增外部模型调用：**0**。

### 适用边界（不要过度解读）

这些是本地回归和公开开发集的结构核对，**不是完整人工金标准确率或官方成绩**。局域网验证覆盖配置和代码，未在第二台物理电脑上做防火墙/网络验收。评测工具防止误把草稿当 gold，**不能证明操作者确实进行了独立人工核验**。多包信息本身有歧义时保留候选和警告，仍需人工处理；不会将候选强行并成一条以制造更好看的计数。

---

## 2026-09-24 · 缺口审查：59 条代码级发现 + 13 条提交物缺口

对全仓做了一次五方向并行审查（抽取链路 / 任务二三 / 前端 / 文档 / 工程质量），产出 [`GAP_ANALYSIS.md`](GAP_ANALYSIS.md)。

其中 **5 条 blocker 经人工在真实代码与数据上复核**（1.1 / 1.2 / 1.3 / 3.1 / 3.2），其余 54 条标记为"审查产出，未独立验证"。

复核过程中推翻了两个此前的判断：

1. **"规则是可靠基线"是错的**——规则在真实公告上既漏又错，模型抽出的才是真标的。
2. **"模型只填了 2/7 个字段"是误诊**——那条公告原文五列全写着「见附件」，模型返回 null 是正确行为。

---

## 2026-09-24 · 模型抽取排障：从 0 条到 19 条

起点：模型抽取**每次请求都拿不到结果**。`clean-compare-3.json` 里 6 次请求全部 `ReadTimeout`，耗时齐刷刷停在 45.7～45.9 秒，成功响应数为 0。

排查过程（每一步都留下了可复用的结论）：

1. **先排除地址与 Key。** 本机模型名 `deepseek-v4-flash`（早期实验用的是 `deepseek-v4-pro`）；连通性测试通过，一次简短 JSON ping 返回 HTTP 200、约 3.4 秒。不是鉴权或地址问题。
2. **把超时从 45 秒提到 120 秒——无效。** 两条测试公告仍各自 `ReadTimeout`（一条 30 条规则表格候选，另一条 3 条）。真因是 `httpx` 的 `timeout` 是**读超时**，非流式下服务端生成完成前一个字节都不发，于是它退化成"整段生成总时长上限"。**任何超时值都救不了非流式请求。**
3. **改为 `httpx.stream` 读 SSE。** 6 次请求全部成功返回——但**抽出来的东西是坏的**：3 条公告里 1 条返回空内容、2 条 `JSONDecodeError`，都是输出被 `max_tokens` 截断的结果。
4. **试过 `thinking: {"type": "disabled"}` 和 `reasoning_effort: none`，两个都适得其反**：推理 token 反而上升（119 → 548），`content` 返回空串，延迟翻三倍。
5. **定位真因。** `deepseek-v4-flash` 是重推理模型，真实公告下会先烧掉约 5000 个推理 token 才输出正文，直接撞穿 `max_tokens=4096`。有效的关闭方式只有 `chat_template_kwargs: {"enable_thinking": false}`；三个顶层同名字段**全部被网关忽略**。

> ⚠️ **一个陷阱**：第 4 步里"不带任何参数看起来正常"（11.02s / 119 推理 token）是在**十几个 token 的短 ping** 上测的。换成真实公告（12000 字符）结论完全相反。**别用短 prompt 下结论。**

结果（同一份语料、同一个模型、同样 6 次请求）：

| 指标 | 修复前 | 修复后 |
|---|---|---|
| `model` 模式标的数 | 0 | **19** |
| `model` 模式主体数 | 0 | **2** |
| `hybrid` 模式标的数 | 32 | **51** |
| 输出 token | 24576 | 5494 |
| 总耗时 | 1216.3s | **291.6s** |
| 单条耗时 | 210 / 216 / 200s | 24.5 / 35.9 / 88.7s |
| 抽取成功的公告数 | **0 / 3** | **3 / 3** |

两份原始结果都在 [`benchmarks/`](benchmarks/)：`stream-compare-3-reasoning-on.json`（即上表"修复前"）与 `stream-compare-3.json`。

**计费**：排障期间用 `model_call_budget` 限制了请求次数，但超时请求是否计费取决于服务商，需由团队查看服务商 usage/billing。修复后的 6 次请求都有本地 token usage 可核验。

对应提交：`a95be2f`（流式改造）、`5763752`（关闭推理）。

---

## 2026-09-24 · 其它提交

| 提交 | 内容 |
|---|---|
| `8eb05e9` | 写面向 AI agent 的接手简报（2026-09-25 整理文档时并入 `ONBOARDING.md`） |
| `04755cf` | 把"字段覆盖不全"拆成真 bug（`total_price`）与附件缺失（「见附件」）两种情况 |
| `ae7e6b3` | 新增缺口分析文档 |
| `1b0de1d` | 同步文档：记录已验证的抽取结果，修正修复叙述 |
| `b71351e` | 实验脚本改为**先检查输出文件再跑**，并加 `--force`；写入改为原子替换 |
| `8d51365` | `.gitignore` 加 `.vite/` |

---

## 2026-09-24 · 开发集采集

按中标/成交公告路径采集开发集：站点当时列表提供 20 条公告，清单位于本机 `backend/.data/development-clean/`，**原始文件不纳入 Git**。旧目录保留作为审计记录，但包含误发现的站内资讯，不用于实验。
