# If reviewers open the UI from another computer on the same LAN, point this
# at the demo machine's backend address. Without this value, the UI uses the
# same hostname as the page with port 8000.
# Leave blank for same-host port 8000. Example override: http://192.168.1.10:8000
VITE_API_BASE=
